<UserControl x:Class="diplomApp.View.ListPage.Card"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:diplomApp.View.ListPage"
             mc:Ignorable="d" 
             MaxWidth="450">
    <Grid Margin="10">
        <Border x:Name="ValueBorder" Margin="5,5,0,0" Background="#ffa6a6" BorderBrush="Black" BorderThickness="2">
            <TextBox Text="{Binding BoxValue, RelativeSource={RelativeSource AncestorType=UserControl}}"  
                     IsReadOnly="{Binding IsReadOnly, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                     Background="Transparent" BorderThickness="0" FontFamily="Arial Black" MaxWidth="300" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="22" TextWrapping="Wrap" Margin="50,35,50,30"/>
        </Border>
        <Border Padding="2" Background="#f2f2f2" BorderBrush="Black" BorderThickness="2" VerticalAlignment="Top" HorizontalAlignment="Left">
            <TextBlock  Text="{Binding BoxCaption, RelativeSource={RelativeSource AncestorType=UserControl}}" FontWeight="Medium" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="5,0,5,0" FontSize="18"/>
        </Border>
    </Grid>
</UserControl>
