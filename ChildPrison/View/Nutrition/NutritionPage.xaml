<Page x:Class="diplomApp.View.NutritionPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="900" d:DesignWidth="1000"
      Title="NutritionPage"
      Background="White">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Левая панель - навигация по разделам -->
        <Border BorderThickness="0,0,3,0" BorderBrush="#1a1a1a" Background="#f5f5f5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border BorderThickness="0,0,0,3" BorderBrush="#1a1a1a" Background="#1a1a1a">
                    <TextBlock Text="ПАНЕЛЬ ПИТАНИЯ" 
                               FontSize="22" 
                               FontWeight="Black"
                               Foreground="White"
                               HorizontalAlignment="Center"
                               Margin="0,15,0,15"
                               TextWrapping="Wrap"
                               TextAlignment="Center"/>
                </Border>

                <StackPanel Grid.Row="1" Margin="15,20,15,10">
                    <RadioButton x:Name="ProductsRadio" Content="ПРОДУКТЫ" 
                                 Style="{StaticResource MenuRadioButton}"
                                 Checked="ProductsRadio_Checked"/>
                    <RadioButton x:Name="SuppliersRadio" Content="ПОСТАВЩИКИ" 
                                 Style="{StaticResource MenuRadioButton}"
                                 Checked="SuppliersRadio_Checked"/>
                    <RadioButton x:Name="DishesRadio" Content="БЛЮДА" 
                                 Style="{StaticResource MenuRadioButton}"
                                 Checked="DishesRadio_Checked"/>
                    <RadioButton x:Name="MenuRadio" Content="МЕНЮ" 
                                 Style="{StaticResource MenuRadioButton}"
                                 Checked="MenuRadio_Checked"/>
                </StackPanel>

                <Border Grid.Row="2" BorderThickness="0,2,0,0" BorderBrush="#1a1a1a" Margin="15,0,15,20">
                    <StackPanel Margin="0,15,0,0">
                        <TextBlock Text="СТАТИСТИКА" 
                                   FontWeight="Black" 
                                   FontSize="12"
                                   Foreground="#666"
                                   />
                        <TextBlock x:Name="StatsText" 
                                   FontSize="11" 
                                   Foreground="#333"
                                   Margin="0,10,0,0"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Правая панель - контент -->
        <Border Grid.Column="1" Background="White">
            <Frame x:Name="ContentFrame" NavigationUIVisibility="Hidden"/>
        </Border>
    </Grid>
</Page>